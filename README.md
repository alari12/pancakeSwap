Support bot that asks for public wallet addresses and returns public on-chain info via BscScan.

Setup:
- Copy `.env.example` to `.env` and fill TELEGRAM_TOKEN
- pip install -r requirements.txt
- Run: python bot.py
